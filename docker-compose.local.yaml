services:
  app:
    build:
      context: .
      dockerfile: docker/frankenphp/Dockerfile
    image: hps/dashboard:local
    container_name: hps_app_local
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
