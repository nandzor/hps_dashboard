services:
  app:
    build:
      context: .
      dockerfile: docker/frankenphp/Dockerfile
    image: hps/dashboard:staging
    container_name: hps_app_staging
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
