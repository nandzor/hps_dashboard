services:
  app:
    build:
      context: .
      dockerfile: docker/frankenphp/Dockerfile
    image: hps/dashboard:dev
    container_name: hps_app_dev
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
